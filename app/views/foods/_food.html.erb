<div>
  <p>【メールアドレス】<%= food.user.email %></p>
  <p>【料理名】<%= food.name %></p>
  <p>
   <%= link_to "詳細", food %>
   <% if current_user.id == food.user_id %>
    <%= link_to "編集", edit_food_path(food) %>
    <%= link_to "削除", food_path(food), data: { turbo_method: :delete, turbo_confirm: "削除してもよろしいでしょうか？" }%>
   <% end %>
  </p>
</div>
<hr>
